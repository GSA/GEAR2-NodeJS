'use strict';

describe('apps.graveyard module', function() {

  beforeEach(module('apps.graveyard'));

  describe('apps graveyard controller', function() {

    it('should ....', inject(function($controller) {
      //spec body
      var appsGraveyardCtrl = $controller('AppsGraveyardCtrl');
      expect(appsGraveyardCtrl).toBeDefined();
    }));

  });
});
