export const isSelf = (value, allValues) => {
  console.log(value)
  // console.log(allValues)
  //   if (value === allValues.id) {
  //       return 'Cannot use yourself in this field';
  //   }
    return [];
}
